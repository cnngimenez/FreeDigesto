<%= form_with(model: patron_privilegio, local: true) do |form| %>
  <% if patron_privilegio.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(patron_privilegio.errors.count, "error") %> prohibited this patron_privilegio from being saved:</h2>

      <ul>
      <% patron_privilegio.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="field">
    <%= form.label :comentario %>
    <%= form.text_area :comentario %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

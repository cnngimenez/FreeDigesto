<%= form_with(model: descriptor_general, local: true) do |form| %>
  <% if descriptor_general.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(descriptor_general.errors.count, "error") %> prohibited this descriptor_general from being saved:</h2>

      <ul>
      <% descriptor_general.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="field">
    <%= form.label :comentario %>
    <%= form.text_area :comentario %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

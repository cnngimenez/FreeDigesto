<%= form_with(model: posee_descriptor, local: true) do |form| %>
  <% if posee_descriptor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(posee_descriptor.errors.count, "error") %> prohibited this posee_descriptor from being saved:</h2>

      <ul>
      <% posee_descriptor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :norma_id %>
    <%= form.text_field :norma_id %>
  </div>

  <div class="field">
    <%= form.label :descriptor_general_id %>
    <%= form.text_field :descriptor_general_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

<%= form_with(model: lugar_publicacion, local: true) do |form| %>
  <% if lugar_publicacion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(lugar_publicacion.errors.count, "error") %> prohibited this lugar_publicacion from being saved:</h2>

      <ul>
      <% lugar_publicacion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="field">
    <%= form.label :comentario %>
    <%= form.text_area :comentario %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

<%= form_with(model: tipo_norma, local: true) do |form| %>
  <% if tipo_norma.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tipo_norma.errors.count, "error") %> prohibited this tipo_norma from being saved:</h2>

      <ul>
      <% tipo_norma.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="field">
    <%= form.label :comentario %>
    <%= form.text_area :comentario %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

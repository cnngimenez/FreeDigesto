<%= form_with(model: descriptor_particular, local: true) do |form| %>
  <% if descriptor_particular.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(descriptor_particular.errors.count, "error") %> prohibited this descriptor_particular from being saved:</h2>

      <ul>
      <% descriptor_particular.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="field">
    <%= form.label :comentario %>
    <%= form.text_area :comentario %>
  </div>

  <div class="field">
    <%= form.label :descriptor_general_id %>
    <%= form.text_field :descriptor_general_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

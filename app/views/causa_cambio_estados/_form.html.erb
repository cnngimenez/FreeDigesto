<%= form_with(model: causa_cambio_estado, local: true) do |form| %>
  <% if causa_cambio_estado.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(causa_cambio_estado.errors.count, "error") %> prohibited this causa_cambio_estado from being saved:</h2>

      <ul>
      <% causa_cambio_estado.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="field">
    <%= form.label :comentario %>
    <%= form.text_area :comentario %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

<%= form_with(model: privilegio, local: true) do |form| %>
  <% if privilegio.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(privilegio.errors.count, "error") %> prohibited this privilegio from being saved:</h2>

      <ul>
      <% privilegio.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="field">
    <%= form.label :comentario %>
    <%= form.text_area :comentario %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
